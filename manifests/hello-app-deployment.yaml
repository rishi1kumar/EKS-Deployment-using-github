apiVersion: app/v1 
kind: Deployment
metadata:
  name: kubernetes-demo-hello-app
  labels: 
     app: hello-app

spec:
  replicas: 1 
  selector:
    matchLabels:
      app: hello-app

  template:
    metadata:
      labels:
        app: hello-app
    spec:
      containers:
      - name: hello-app
        image: 002748133292.dkr.ecr.ap-south-1.amazonaws.com/rishi-ecr-private12:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
